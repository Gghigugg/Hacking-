const uiStatus=document.getElementById('status');const support=document.createElement('div');support.className='support-pill';support.innerHTML='<span class="support-dot"></span><span>WEBGL</span><b>CHECKING</b>';document.querySelector('.topbar').appendChild(support);function checkGraphics(){let ok=false;try{const c=document.createElement('canvas');ok=!!(c.getContext('webgl2')||c.getContext('webgl'))}catch(e){}support.querySelector('b').textContent=ok?'READY':'UNAVAILABLE';support.classList.toggle('bad',!ok)}checkGraphics();const progress=document.createElement('div');progress.className='benchmark-progress glass';progress.innerHTML='<div class="progress-head"><span>BENCHMARK</span><b id="progressValue">READY</b></div><div class="progress-track"><i id="progressBar"></i></div>';document.querySelector('.result').after(progress);window.LAGLAB_UI={progress(v,label){progress.querySelector('#progressValue').textContent=label||Math.round(v)+'%';progress.querySelector('#progressBar').style.width=Math.max(0,Math.min(100,v))+'%'}};